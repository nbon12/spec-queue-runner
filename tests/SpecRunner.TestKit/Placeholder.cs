namespace SpecRunner.TestKit;

/// <summary>
/// Placeholder so the TestKit assembly compiles in Phase 1. Real assets — the fake
/// claude recorder (T016–T020), fixture repos, and the in-memory GitHub client —
/// replace this in Phase 2.
/// </summary>
internal static class Placeholder;
